/* Coleções de Elementos */
// alert (document.title)
// alert (document.images.length)

// var img = document.images;
// for (var i=0; i < img.length; i++){
    // alert (img[i].alt);
// }

// alert(img[0]);
// alert(img.item(0));
// alert(img.namedItem("selecionada"));

// alert (document.links[0]);
// Esse raciocínio serve para adicionar e remover qualquer tipo de coisa (formulários, links, imagens, coleções em geral etc.).
// Ver esta aula outra vez